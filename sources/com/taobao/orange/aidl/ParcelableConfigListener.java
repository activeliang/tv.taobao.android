package com.taobao.orange.aidl;

import android.os.Binder;
import android.os.IBinder;
import android.os.IInterface;
import android.os.Parcel;
import android.os.RemoteException;
import java.util.Map;

public interface ParcelableConfigListener extends IInterface {
    void onConfigUpdate(String str, Map map) throws RemoteException;

    public static abstract class Stub extends Binder implements ParcelableConfigListener {
        private static final String DESCRIPTOR = "com.taobao.orange.aidl.ParcelableConfigListener";
        static final int TRANSACTION_onConfigUpdate = 1;

        public Stub() {
            attachInterface(this, DESCRIPTOR);
        }

        public static ParcelableConfigListener asInterface(IBinder obj) {
            if (obj == null) {
                return null;
            }
            IInterface iin = obj.queryLocalInterface(DESCRIPTOR);
            if (iin == null || !(iin instanceof ParcelableConfigListener)) {
                return new Proxy(obj);
            }
            return (ParcelableConfigListener) iin;
        }

        public IBinder asBinder() {
            return this;
        }

        public boolean onTransact(int code, Parcel data, Parcel reply, int flags) throws RemoteException {
            switch (code) {
                case 1:
                    data.enforceInterface(DESCRIPTOR);
                    onConfigUpdate(data.readString(), data.readHashMap(getClass().getClassLoader()));
                    reply.writeNoException();
                    return true;
                case 1598968902:
                    reply.writeString(DESCRIPTOR);
                    return true;
                default:
                    return super.onTransact(code, data, reply, flags);
            }
        }

        private static class Proxy implements ParcelableConfigListener {
            private IBinder mRemote;

            Proxy(IBinder remote) {
                this.mRemote = remote;
            }

            public IBinder asBinder() {
                return this.mRemote;
            }

            public String getInterfaceDescriptor() {
                return Stub.DESCRIPTOR;
            }

            public void onConfigUpdate(String groupName, Map args) throws RemoteException {
                Parcel _data = Parcel.obtain();
                Parcel _reply = Parcel.obtain();
                try {
                    _data.writeInterfaceToken(Stub.DESCRIPTOR);
                    _data.writeString(groupName);
                    _data.writeMap(args);
                    this.mRemote.transact(1, _data, _reply, 0);
                    _reply.readException();
                } finally {
                    _reply.recycle();
                    _data.recycle();
                }
            }
        }
    }
}
